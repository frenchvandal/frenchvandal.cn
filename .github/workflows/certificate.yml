name: Issue a Google certificate
on:
  workflow_dispatch:
  push:
    branches: master
    paths:
      - ".github/workflows/certificate.yml"
env:
  Ali_Key: ${{ secrets.Ali_Key }}
  Ali_Secret: ${{ secrets.Ali_Secret }}

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Install acme.sh
        run: |
          curl https://get.acme.sh | sh
        shell: bash
      - name: Emit certificate
        run: |      
          ~/.acme.sh/acme.sh --register-account -m philippe.ribeiro@gmail.com --server google --eab-kid 660f7aa66f86ab98c8ddab33673de4ee --eab-hmac-key rOPzK1QC7ERAAoERTdBPc0FZgcTpaWg4C-bepyS3-mwVjcusKs8dmtT84xXJLynFybIY5ZCQb9R_bF5HN-8tsw
          ~/.acme.sh/acme.sh --issue --server google -d frenchvandal.cn -d www.frenchvandal.cn --dns dns_ali
        shell: bash
      - name: Upload certificate
        uses: actions/upload-artifact@v3
        with:
         name: certificate
         path: |
           /home/runner/.acme.sh/
