name: Issue a Google certificate
on:
  workflow_dispatch:
  push:
    branches: master
    paths:
      - ".github/workflows/certificate.yml"
env:
  Ali_Key: ${{ secrets.Ali_Key }}
  Ali_Secret: ${{ secrets.Ali_Secret }}

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Install acme.sh
        run: |
          curl https://get.acme.sh | sh
        shell: bash
      - name: Emit certificate
        run: |      
          ~/.acme.sh/acme.sh --register-account -m philippe.ribeiro@gmail.com --server google --eab-kid 660f7aa66f86ab981e5dedfd8ab46e3d --eab-hmac-key evZtIbVgNNW_HAYfUDFwyCdY3YWOTK90rhhr78ctz3TwVtsX5n0u3ihC-rX5RF5kixuoi-XH3EgANW8pJShDfQ --debug 2
          ~/.acme.sh/acme.sh --issue --server google -d frenchvandal.cn -d www.frenchvandal.cn --dns dns_ali
        shell: bash
      - name: Upload certificate
        uses: actions/upload-artifact@v3
        with:
         name: certificate
         path: |
           /home/runner/.acme.sh/
